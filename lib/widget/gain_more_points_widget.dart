import 'package:flutter/material.dart';

class GainMorePoints extends StatelessWidget {
  const GainMorePoints({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
